# ==============================================================================
# Manul Core Admin - Environment Configuration
# ==============================================================================
#
# Copy this file to .env and customize for your environment.
# NEVER commit .env to version control!
#
# Seules les variables qui changent par déploiement sont ici.
# Les valeurs fixes (session timeout, etc.) sont dans le code.
#
# ==============================================================================

# URL absolue du backend Rust
PUBLIC_API_URL=http://localhost:8080

# URL WebSocket
PUBLIC_WS_URL=ws://localhost:8080/ws

# Admin API secret — injecté côté serveur dans le header X-Admin-Secret.
# DOIT correspondre au ADMIN_SECRET du backend Rust.
# Le navigateur n'y a JAMAIS accès.
ADMIN_SECRET=

# ==============================================================================
# SSR RUNTIME (SvelteKit Node adapter)
# ==============================================================================

NODE_ENV=development
PORT=5174
HOST=0.0.0.0
ORIGIN=http://localhost:5174
